/** @type {import('prettier').Config} */
const config = {
  arrowParens: 'always',
  bracketSpacing: true,
  trailingComma: 'all',
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  printWidth: 90,
}

export default config
